[kafka_consumer]
consumer_group = "kafkaproxy"
extended_logging = true
brokers = ["localhost:9092"]
topics = ["topic1", "topic2"]

[kafka_producer]
# Extended logging for Kafka client
extended_logging = true

# The comma-separated list of brokers in the Kafka cluster
brokers = ["localhost:9092"]

[cassandra]
hosts = ["host1", "host2"]
keyspace = "test"

[monitoring]
server = "localhost:2005"
prefix = "test"
